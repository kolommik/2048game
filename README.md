# 2048 Game - Reinforcement Learning Project

## Описание

Учебный проект для изучения Reinforcement Learning на примере игры 2048. Включает в себя движок игры, различные базовые агенты и современный веб-интерфейс для визуализации и анализа.

## Компоненты

### 1. Game Engine (`game2048_engine.py`)

Основной движок игры с полной реализацией логики 2048:

- **Класс Game2048**:
  - Матрица 4x4 для игрового поля
  - Движения в 4 направлениях (UP, DOWN, LEFT, RIGHT)
  - Детерминированный режим (с seed) для воспроизводимости
  - Методы для копирования состояния (для симуляций)
  - Проверка окончания игры и доступных ходов

**Основные методы:**

- `reset()` - начать новую игру
- `move(direction)` - сделать ход
- `get_available_moves()` - получить список валидных ходов
- `get_state()` / `set_state()` - сохранение/восстановление состояния
- `copy()` - создать копию игры для симуляций

### 2. Агенты (`agents_2048.py`)

Базовый класс `Agent2048` и несколько реализаций:

#### RandomAgent

- Выбирает случайный валидный ход
- Baseline для сравнения
- Средний счет: ~1100

#### CornerAgent

- Старается держать максимальную плитку в углу
- Использует приоритеты направлений
- Средний счет: ~2200

#### GreedyAgent

- Выбирает ход с максимальным немедленным выигрышем
- Простая жадная стратегия
- Средний счет: ~4400

#### MonotonicAgent

- Старается поддерживать монотонность рядов и столбцов
- Использует несколько эвристик:
  - Монотонность (упорядоченность значений)
  - Гладкость (минимизация разницы между соседями)
  - Количество пустых клеток
- Средний счет: ~5000
- **Лучший из базовых агентов**

### 3. Streamlit Web Interface (`streamlit_2048.py`) ✨

Веб-интерфейс с полным функционалом:

#### 🎮 Play Manually
- Интерактивная игра с кнопками управления
- Режим редактирования доски (ручная установка значений ячеек)
- Сохранение состояния игры
- Цветная визуализация в стиле оригинальной игры 2048

#### 🤖 Watch Agents
- Выбор агента из списка доступных
- Настройка количества игр для симуляции
- Опциональная визуализация игрового процесса в реальном времени
- Автоматический сбор статистики с метриками

#### ⚔️ Compare Agents
- Одновременное сравнение нескольких агентов
- Настраиваемое количество игр для каждого агента
- Сводная таблица результатов
- Автоматическое определение победителя

#### 📊 Analytics
- Интерактивные графики с Plotly:
  - Распределение максимальных плиток (128, 256, 512, 1024, 2048, >2048)
  - Прогрессия счета во время игр
  - Статистические метрики
- Экспорт данных в CSV
- Хранение истории симуляций

**Особенности UI:**
- Настраиваемые размеры через константы `UIConfig`
- Компактное центрированное отображение
- Современный дизайн с цветовой схемой оригинальной игры

### 4. Терминальный интерфейс (`terminal_2048.py`)

Интерактивный интерфейс для консоли:

- Ручной игры (WASD управление)
- Наблюдения за игрой агентов
- Быстрого сравнения агентов

**Функции:**

- Цветная визуализация плиток
- Отображение счета и статистики
- Возможность Undo (отмена хода)
- Регулируемая скорость просмотра игры агента

## Установка

### Требования

- Python 3.8+
- uv (рекомендуется для управления зависимостями)

### Установка с помощью uv (рекомендуется)

```bash
# Установка uv (если еще не установлен)
# Windows (PowerShell)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установка зависимостей
uv pip install -r requirements.txt
```

### Альтернативная установка с pip

```bash
pip install -r requirements.txt
```

### Обновление зависимостей

Проект использует `requirements.in` для управления зависимостями:

```bash
# 1. Отредактируйте requirements.in (добавьте/удалите/измените пакеты)

# 2. Перекомпилируйте requirements.txt
uv pip compile requirements.in -o requirements.txt

# 3. Установите обновленные зависимости
uv pip install -r requirements.txt
```

## Использование

### Streamlit Web Interface (рекомендуется)

```bash
streamlit run src/streamlit_2048.py
```

Откроется браузер с полнофункциональным интерфейсом на `http://localhost:8501`

**Возможности:**
- Играть вручную или в режиме редактирования
- Наблюдать за агентами с визуализацией
- Сравнивать производительность разных стратегий
- Анализировать результаты с интерактивными графиками
- Экспортировать данные для дальнейшего анализа

### Запуск движка напрямую

```python
from game2048_engine import Game2048, Direction

# Создать игру
game = Game2048(seed=42)  # seed для воспроизводимости

# Сделать ход
valid, points = game.move(Direction.UP)

# Получить доступные ходы
moves = game.get_available_moves()

# Проверить состояние
if game.game_over:
    print(f"Game Over! Score: {game.score}")
```

### Использование агентов

```python
from agents_2048 import MonotonicAgent
from game2048_engine import Game2048

agent = MonotonicAgent()
game = Game2048()

# Играть одну партию
result = agent.play_game(game)
print(f"Score: {result['final_score']}")
print(f"Max tile: {result['max_tile']}")
```

### Терминальный интерфейс

```bash
# Запустить главное меню
python terminal_2048.py

# Играть вручную
python terminal_2048.py play

# Наблюдать за агентом
python terminal_2048.py watch

# Быстрое сравнение агентов
python terminal_2048.py compare
```

## Добавление новых агентов

Подробная инструкция для разработчиков находится в файле `HOW_TO_ADD_NEW_AGENT.md`.

**Краткий процесс:**
1. Создайте класс, наследующийся от `Agent2048`
2. Реализуйте метод `choose_action()`
3. Добавьте импорт в `streamlit_2048.py`
4. Добавьте агента в UI (Watch Agents и Compare Agents)

## Результаты базовых агентов

На основе 50 игр с детерминированными seed'ами:

| Агент                 | Средний счет | Лучший счет | Лучшая плитка |
| --------------------- | ------------ | ----------- | ------------- |
| Random                | 1,163        | 2,904       | 256           |
| Corner (top-left)     | 2,159        | 5,348       | 512           |
| Corner (bottom-right) | 2,245        | 4,440       | 256           |
| Greedy                | 4,430        | 17,556      | 1024          |
| **Monotonic**         | **4,997**    | **18,408**  | **1024**      |

## Roadmap

### ✅ Фаза 1: Базовая инфраструктура (Завершена)

- [x] Движок игры с детерминизмом
- [x] Базовые эвристические агенты
- [x] Терминальный интерфейс
- [x] Веб-интерфейс на Streamlit
- [x] Система аналитики и визуализации

### 🎯 Фаза 2: Deep Reinforcement Learning

**Цель:** Реализация агента на основе Deep Q-Network

- [ ] Создать Gym-совместимый environment
- [ ] Реализовать DQN агента с базовой архитектурой
- [ ] Эксперименты с представлениями состояния:
  - One-hot encoding
  - Log-преобразование значений плиток
  - Нормализация
- [ ] Reward shaping и настройка гиперпараметров
- [ ] Интеграция обученного агента в Streamlit интерфейс
- [ ] Визуализация процесса обучения

### 🚀 Фаза 3: Advanced RL Algorithms

**Цель:** Улучшенные алгоритмы и их сравнение

- [ ] Double DQN (устранение переоценки Q-values)
- [ ] Dueling DQN (separate value and advantage streams)
- [ ] Prioritized Experience Replay
- [ ] Rainbow DQN (комбинация улучшений)
- [ ] Policy-based методы (A2C, PPO)
- [ ] Сравнение всех алгоритмов в едином бенчмарке

### ⚡ Фаза 4: Оптимизация и масштабирование

**Цель:** Ускорение обучения и экспериментов

- [ ] Векторизованные окружения для параллельного обучения
- [ ] Оптимизированный experience replay buffer
- [ ] GPU-ускорение обучения
- [ ] Distributed training (Ray/RLlib)
- [ ] Профилирование и оптимизация узких мест

### 🎓 Фаза 5: Специализированные подходы

**Цель:** Методы, специфичные для 2048

- [ ] N-tuple networks (доказанно эффективны для 2048)
- [ ] Monte Carlo Tree Search (MCTS)
- [ ] AlphaZero-style обучение
- [ ] Expectimax с нейросетевой эвристикой
- [ ] Комбинированные подходы (RL + MCTS)

### 🔬 Фаза 6: Исследования и расширения

**Цель:** Продвинутые эксперименты и анализ

- [ ] Transfer learning для разных размеров доски (3x3, 5x5, 6x6)
- [ ] Curriculum learning (постепенное усложнение)
- [ ] Интерпретируемость решений (attention, saliency maps)
- [ ] Ablation studies компонентов агентов
- [ ] Сравнение с человеческой игрой
- [ ] Модификации правил игры (другие вероятности плиток, размеры)

## Производительность

### Базовые агенты (скорость симуляции)

- **Random Agent**: ~0.02 сек/игра
- **Corner Agent**: ~0.03 сек/игра
- **Greedy Agent**: ~0.14 сек/игра
- **Monotonic Agent**: ~0.27 сек/игра

Движок оптимизирован для скорости с использованием NumPy операций.

### Веб-интерфейс

- Плавная визуализация игры в реальном времени
- Обработка 20+ игр за раз без задержек
- Интерактивные графики без лагов

## Структура проекта

```
2048game/
├── src/
│   ├── game2048_engine.py      # Движок игры
│   ├── agents_2048.py          # Реализации агентов
│   ├── terminal_2048.py        # Терминальный интерфейс
│   ├── streamlit_2048.py       # Веб-интерфейс
│   └── HOW_TO_ADD_NEW_AGENT.md # Инструкция для разработчиков
├── requirements.in             # Основные зависимости
├── requirements.txt            # Зафиксированные версии
└── README.md                   # Этот файл
```

## Конфигурация

Настройки UI можно изменить в `streamlit_2048.py`:

```python
class UIConfig:
    BOARD_HEIGHT = 280              # Высота доски в пикселях
    BOARD_CELL_FONT_SIZE = 20       # Размер шрифта в ячейках
    BOARD_MAX_WIDTH = 500           # Максимальная ширина доски
    BOARD_COLUMN_WIDTH = "small"    # Ширина колонок
```

## Разработка

### Тестирование агента

```python
from agents_2048 import MonotonicAgent
from game2048_engine import Game2048

# Тест одной игры
agent = MonotonicAgent()
game = Game2048(seed=42)
result = agent.play_game(game, verbose=True)

# Тест множества игр
scores = []
for i in range(100):
    game = Game2048(seed=i)
    result = agent.play_game(game)
    scores.append(result['final_score'])

print(f"Average: {sum(scores) / len(scores):.0f}")
print(f"Best: {max(scores)}")
```

## Автор

Учебный проект для изучения Reinforcement Learning и лучших практик разработки ML-систем.